#%RAML 1.0 Trait
responses:
  404:
    description: <<typeName>> não encontrado
    body:
      application/json:
        type: 
          properties:
            message:
              type: string
              example: "Resource not found"
            details:
              type: string
              example: "No <<typeName>> found with the given identifier"